let credentials_username="storm";
let credentials_password="fortune";

let username="storm";
let password="fortune";
if (credentials_username==username && credentials_password==password){
  console.log("you can login");
}
else{
  console.log("you can't login");
}